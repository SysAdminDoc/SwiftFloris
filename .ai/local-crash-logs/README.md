# Local Crash Logs

This directory is for ignored, machine-local JVM crash logs and replay logs
moved out of the repository root.

Files matching `hs_err_pid*.log` and `replay_pid*.log` remain ignored by the
top-level `*.log` rule. Keep only lightweight notes here under version
control; do not force-add the log files themselves.

<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:settingsActivity="dev.patrickgold.florisboard.SettingsLauncherAlias"
    android:languageSettingsActivity="dev.patrickgold.florisboard.ImeLanguageSettingsAlias"
    android:supportsInlineSuggestions="true"
    android:supportsInlineSuggestionsWithTouchExploration="true"
    android:supportsStylusHandwriting="true"
    android:supportsSwitchingToNextInputMethod="true"
    tools:targetApi="tiramisu">

    <!-- API 33+: advertise the IME callbacks and inline autofill path when TalkBack or
         another touch-exploration service is active. A recognizer addon remains optional. -->

    <!-- Add default system subtype so we can properly set the icon and label -->
    <subtype
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:imeSubtypeMode="keyboard"
        android:imeSubtypeExtraValue="AsciiCapable"
        android:isAsciiCapable="true"
        android:overridesImplicitlyEnabledSubtype="true"/>

</input-method>

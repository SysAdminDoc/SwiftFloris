Audit hardening pass: sync pairing now adopts cluster state and discovers installed QR scanners on Android 11+. Clear-primary clipboard actions preserve pinned media, foreign media clips fail closed, theme loads reject escaping stylesheet paths, and glide geometry caches refresh after relayout. Private typing data paths, import limits, provider startup, and quick-action semantics were tightened.

Audit hardening pass: sync pairing now adopts cluster state and discovers installed QR scanners on Android 11+. Clear-primary clipboard actions preserve pinned media, foreign media clips fail closed, and theme loads reject escaping stylesheet paths while cleaning failed imports. Private typing data paths, import limits, provider startup, and quick-action semantics were tightened.

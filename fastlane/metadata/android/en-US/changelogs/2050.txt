Settings -> Sync can now move personal dictionary changes through the existing no-network file channels. Export writes a sealed sync JSON file to a picked local folder or manual document target for paired devices; import opens envelopes addressed to this device, merges CRDT state, applies inserts/updates/deletes locally, and shows progress, success, no-op, or unchanged-data failure states.

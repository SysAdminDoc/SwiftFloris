An unreadable personal-dictionary file used to crash the keyboard on the next word you typed; it degrades quietly now. A snippet import no longer writes a file you cannot see or delete, re-importing a dictionary stops stacking up duplicates, a custom keypress sound takes effect when you pick it, and an updated layout extension shows without a restart. Android 16 can find the language settings, and Advanced Protection applies without restarting.

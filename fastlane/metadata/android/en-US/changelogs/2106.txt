Privacy and reliability pass: clipboard history is now SQLCipher-encrypted at rest with a verified, reversible migration; the sync identity key is Keystore-wrapped and can no longer be lost by a failed write; MCP daemons report their real binding state with an explicit retry; glide typing steps aside on low-RAM devices and recovers from allocation failures; voice commands match non-Latin scripts; calendar permission denial offers localized recovery; learned-entry failures are retryable; status cards are announced to screen readers; and one data inventory now drives every backup rule.
